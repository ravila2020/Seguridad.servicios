package configuration;

import org.springframework.security.core.Authentication;

public class UsernamePasswordAuthenticationToken {

		static Authentication UsernamePasswordAuthenticationToken(String usuario, String password)
		{
			Authentication authVerify = null;
			authVerify.isAuthenticated();
			authVerify.setAuthenticated(true);
			
			String respuesta = new String();
			
			return authVerify;
		}
	
}
